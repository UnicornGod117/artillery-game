STUFF TO FIX — status

[DONE] 1. unable to change difficulties
        → top-bar difficulty selector (cycles EASY / MEDIUM I / MEDIUM II / HARD)
          regenerates the mission in place. Beam minimum is MEDIUM I.

[DONE] 2. unable to delete stuff drawn on
        → board ERASE tool (click a measurement to delete it) + UNDO button,
          alongside the existing CLR (clear all).

[DONE] 3. grid/map is unintuitive and slightly too minimalist
        → grid on by default with km coordinate labels, cardinal + 30° bearing
          ticks around the outer ring, and an always-on cursor readout
          (range / bearing / E,N).

[DONE] 4. showing more information than is necessary for difficulty
        → environment readouts are now gated by tier: wind only when wind is
          modelled, air density / temp only when drag is on, local g only with
          variable gravity. Easy shows "vacuum · flat ground · constant g".
   ...   unable to change the weapon
        → kinetic munition selector (tap the loaded round to cycle M-72 / M-81).

[DONE] 5. add a little animation of the shell going + cooldown between each shot
        → post-fire fly-out on the board and the vertical plane, plus a real
          reload cooldown (fire button locks while the RELOAD bar refills).

[DONE] 6. a miss shouldn't immediately hand over all the facts
        → calibration shots now read back a SPOTTER (OP-1) report: range &
          bearing to the round from the observation post, and how far off the
          target it looked from there. Triangulate the correction yourself.

[DONE] 7. be able to move around pop-up windows
        → calculator and handbook windows are draggable by their title bar.

[DONE] 8. don't have the gun immediately pointed at the target
        → kinetic azimuth starts at 0° (due north); read the bearing and lay
          the gun yourself.

[DONE] 9. relativistic beam panel told you too much
        → removed the pre-computed Lorentz γ and the "set pulse energy"
          instruction; only the published specs (β, kill threshold) remain.

[DONE] +  calculator crash on arcsine / bad error handling
        → parser has a recursion-depth cap (a StackOverflow is uncatchable and
          would crash the whole app); domain errors (asin/acos/sqrt/ln/log) and
          NaN/∞ results are reported cleanly; the keypad handler can't throw out.

[DONE] +  cannon range too small
        → kinetic targets can now sit out to ~40 km (drag-free tiers); the
          plotting board auto-scales its rings/zoom to the engagement.

[DONE] 10. beam auto-filled the correct bearing on a new mission
        → the emitter now starts at azimuth/elevation 0 (like the kinetic gun).
          Pointing is read off the target coordinate, not handed to you.

[DONE] 11. zoom limits too tight
        → plotting-board zoom now spans 0.35×–24× (was 0.6×–3×) and zooms
          toward the cursor, so you can pull right in to plot by hand.

[DONE] 12. beam: choose particle SPEED, not energy (no "anything more than")
        → you now dial β (% of c); the pulse delivers N·(γ−1)m₀c². The kill is an
          energy WINDOW (required ± tolerance), so you must SOLVE the speed for the
          required energy — too fast over-penetrates and misses. Maxing β loses.

[DONE] 13. gun-as-origin gave away exact bearing & range
        → there's now a gridded battlespace with absolute 3D coordinates. The gun
          and target are placed at coordinates; the target is given as a COORDINATE,
          and bearing / range / elevation are yours to work out. The board, grid
          labels and cursor readout all read in absolute coordinates.
